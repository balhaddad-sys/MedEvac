{
  "rules": {
    "patients": {
      ".read": "auth != null",
      "$unit": {
        ".read": "auth != null",
        ".write": "auth != null",
        ".validate": "$unit.matches(/^[A-E]_(M|F)$/)",
        "$patient": {
          ".validate": "newData.hasChildren(['name', 'civil', 'ward', 'code', 'ts'])",
          "name": { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 200" },
          "civil": { ".validate": "newData.isString() && newData.val().length <= 20" },
          "nat": { ".validate": "newData.isString() && newData.val().length <= 50" },
          "ward": { ".validate": "newData.isString() && newData.val().length <= 30" },
          "code": { ".validate": "newData.isNumber() && newData.val() >= 1 && newData.val() <= 3" },
          "room": { ".validate": "newData.isString() && newData.val().length <= 30" },
          "notes": { ".validate": "newData.isString() && newData.val().length <= 500" },
          "doctor": { ".validate": "newData.isString() && newData.val().length <= 100" },
          "diagnosis": { ".validate": "newData.isString() && newData.val().length <= 200" },
          "category": { ".validate": "newData.isString() && newData.val().length <= 30" },
          "ts": { ".validate": "newData.isNumber()" },
          "$other": { ".validate": false }
        }
      }
    },
    "teams": {
      ".read": "auth != null",
      "$unit": {
        ".read": "auth != null",
        ".write": "auth != null",
        ".validate": "$unit.matches(/^[A-E]$/)",
        "$doctor": {
          ".validate": "newData.hasChildren(['name', 'ts'])",
          "name": { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 100" },
          "specialty": { ".validate": "newData.isString() && newData.val().length <= 100" },
          "ts": { ".validate": "newData.isNumber()" },
          "$other": { ".validate": false }
        }
      }
    },
    "pins": {
      ".read": false,
      ".write": false
    },
    "config": {
      ".read": false,
      ".write": false,
      "minAppVersion": { ".read": true },
      "appStoreURL": { ".read": true }
    },
    "backups": {
      ".read": false,
      ".write": false
    },
    "audit": {
      ".read": false,
      ".write": "auth != null",
      "$entry": {
        ".validate": "newData.hasChildren(['action', 'unit', 'ts']) && newData.child('action').isString() && newData.child('ts').isNumber()",
        "$field": {
          ".validate": "newData.isString() || newData.isNumber()"
        }
      }
    },
    "$other": {
      ".read": false,
      ".write": false
    }
  }
}
